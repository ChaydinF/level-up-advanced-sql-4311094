SELECT sql 
FROM sqlite_schema 
WHERE name = 'employee';

SELECT sql 
FROM sqlite_schema 
WHERE name = 'sales';

SELECT sql 
FROM sqlite_schema 
WHERE name = 'inventory';

SELECT sql 
FROM sqlite_schema 
WHERE name = 'model';

SELECT sql 
FROM sqlite_schema 
WHERE name = 'customer';